// Compat shim — original page split into focused modules under
// `src/components/app-health-check/` and renamed to `appHealthCheckPage.tsx`.
// Any external import of this module still works.
export { default } from './appHealthCheckPage';
